import { Theme, createStyles } from '@material-ui/core'

const styles = (theme: Theme) =>
  createStyles({
      header : {
          
      },
      title : {

      },
      icon : {
          
      }
  })

export default styles